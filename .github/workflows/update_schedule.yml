name: Update Twitch Clips

on:
  schedule:
    # Запускать каждые 6 часов
    - cron: '0 */6 * * *'
  workflow_dispatch: # Позволяет запустить вручную кнопку в GitHub

jobs:
  update-clips:
    runs-on: ubuntu-latest
    permissions:
      contents: write # Разрешаем роботу обновлять файлы
    
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Setup Node.js
        uses: actions/setup-node@v3
        with:
          node-version: '18'

      - name: Run Updater Script
        env:
          TWITCH_CLIENT_ID: ${{ secrets.TWITCH_CLIENT_ID }}
          TWITCH_CLIENT_SECRET: ${{ secrets.TWITCH_CLIENT_SECRET }}
        run: node update-clips.js

      - name: Commit and Push changes
        run: |
          git config --global user.name 'DaymonBot'
          git config --global user.email 'bot@daymon.site'
          git add assets/clips.json
          # Если изменений нет, коммит не создастся (и не упадет с ошибкой)
          git commit -m "🤖 Auto-update clips list" || exit 0
          git push
